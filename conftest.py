# Ensures the repository root is on sys.path so `pytest` (invoked bare,
# as CI does) can import the project package. Equivalent to running
# `python -m pytest`, but works regardless of how pytest is launched.
